export function TermsDiff({terms}:{terms:Record<string,unknown>}){const list=(v:unknown)=>Array.isArray(v)?v.map(String).join(", "):"";return <div className="contract-terms-grid"><div><strong>Role</strong><p>{String(terms.role??"")}</p></div><div><strong>Boundaries</strong><p>{list(terms.boundaries)}</p></div><div><strong>Collaborators</strong><p>{list(terms.collaborators)}</p></div><div><strong>Compensation</strong><p>{String(terms.compensation??"")}</p></div><div><strong>Distribution</strong><p>{String(terms.distributionScope??"")}</p></div><div><strong>Rights</strong><p>{String(terms.rightsScope??"")}</p></div><div><strong>Schedule</strong><p>{String(terms.schedule??"")}</p></div><div><strong>Cancellation</strong><p>{String(terms.cancellation??"")}</p></div></div>}
