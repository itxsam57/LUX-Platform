name: Engineering Gate

on:
  pull_request:
  push:
    branches:
      - main
      - "engineering/**"
      - "feature/**"
      - "fix/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: engineering-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quick:
    name: cheap-prerequisite-gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v6
        with:
          version: 10.15.0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Frozen dependency install
        run: pnpm install --frozen-lockfile

      - name: Run cheap engineering prerequisites
        run: pnpm verify:quick && pnpm security:dependencies && pnpm runtime:dependencies

  verify:
    name: full-verification-gate
    needs: quick
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v6
        with:
          version: 10.15.0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Frozen dependency install
        run: pnpm install --frozen-lockfile

      - uses: supabase/setup-cli@v3
        with:
          version: 2.111.0

      - name: Start isolated Supabase test stack
        run: supabase start

      - name: Export isolated Supabase environment
        shell: bash
        run: |
          eval "$(supabase status -o env)"
          {
            echo "NEXT_PUBLIC_SUPABASE_URL=$API_URL"
            echo "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=$ANON_KEY"
            echo "SUPABASE_SERVICE_ROLE_KEY=$SERVICE_ROLE_KEY"
            echo "NEXT_PUBLIC_APP_URL=http://127.0.0.1:30002"
            echo "AGE_ASSURANCE_MODE=self_attestation"
            echo "RUN_DATABASE_TESTS=1"
          } >> "$GITHUB_ENV"

      - name: Resolve verification comparison base
        shell: bash
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          if [ -n "$PR_BASE_SHA" ]; then
            base="$PR_BASE_SHA"
          elif [ "$GITHUB_REF_NAME" != "main" ]; then
            base="$(git merge-base HEAD origin/main)"
          elif [ -n "$PUSH_BEFORE_SHA" ] && ! [[ "$PUSH_BEFORE_SHA" =~ ^0+$ ]]; then
            base="$PUSH_BEFORE_SHA"
          else
            base="$(git rev-parse HEAD^ 2>/dev/null || git rev-parse HEAD)"
          fi
          echo "HANDOFF_BASE_SHA=$base" >> "$GITHUB_ENV"
          echo "Verification comparison base: $base"

      - name: Detect browser-test impact
        id: impact
        run: node scripts/engineering/ci-impact.mjs

      - name: Run applicable full verification gate
        env:
          SKIP_BROWSER: ${{ steps.impact.outputs.browser_required == 'true' && '0' || '1' }}
        run: pnpm verify:full

      - name: Upload failure evidence
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: engineering-failure-evidence
          path: |
            .engineering/reports/
            apps/web/playwright-report/
            apps/web/test-results/
          if-no-files-found: ignore
          retention-days: 7

      - name: Publish manual-test handoff
        if: always()
        shell: bash
        run: |
          if [ -f .engineering/reports/manual-test-handoff.md ]; then
            cat .engineering/reports/manual-test-handoff.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "NOT READY — AUTOMATED ENGINEERING GATE FAILED" >> "$GITHUB_STEP_SUMMARY"
            echo "The handoff report was not generated." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Stop isolated Supabase test stack
        if: always()
        run: supabase stop --no-backup
